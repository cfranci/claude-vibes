{
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "enter": "chat:submit",
        "cmd+enter": "chat:newline",
        "ctrl+k ctrl+p": "chat:submit:git push"
      }
    }
  ]
}
