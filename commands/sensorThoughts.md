Edit ~/.claude/settings.json and replace the current `spinnerVerbs` block with a clean/SFW version. Keep all the non-vulgar funny ones (like "Fliquoring", "Per-my-last-emailing", "Gaslighting the compiler", "Staring into the abyss", etc.) but remove anything sexual, profane, or NSFW. Set mode to "append". Do NOT ask for confirmation — just do it. After editing, tell the user "Thoughts censored. Run /unsensorThoughts to bring back the chaos."
